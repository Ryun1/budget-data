# Database Configuration
# Note: Using port 5433 to avoid conflict with local PostgreSQL on 5432
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/treasury_data

# API Configuration
RUST_LOG=info
API_PORT=8080

# Indexer Configuration (used in application.properties)
# These are set in indexer/application.properties instead
